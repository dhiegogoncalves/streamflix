plugins {
    id 'java'
}

group = 'com.dev.catalog.admin.infrastructure'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation(project(":domain"))
    implementation(project(":application"))
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.0'
}
